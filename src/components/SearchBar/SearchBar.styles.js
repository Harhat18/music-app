import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    backgroundColor: "#eceff1",
    padding: 18,
    margin: 15,
    borderRadius: 5,
    fontSize: 20,
  },
});
